# Use `bun`

```sh
$ bun dev
```

# Update location `curl` command

```sh
$ curl -X POST http://localhost:3001/v2/location/update \
  -H "Content-Type: application/json" \
  -H "Authorization: sigmaSigmaSigma" \
  -d '{"city":"Paris","country":"France"}'
```
